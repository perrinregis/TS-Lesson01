"use strict";
let a = 'hello';
let b = a;
const addOrConcat = (a, b, c) => {
    if (c === 'add') {
        return a + b;
    }
    else {
        return '' + a + b;
    }
};
let myVal = addOrConcat(2, 2, 'contact');
let myVal1 = addOrConcat(2, 2, 'contact');
// 
const img = document.querySelector('img');
const myImg = document.getElementById('#img');
img.src;
myImg.src;
